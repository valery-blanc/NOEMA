---
import HeroText from './HeroText.astro'
import Values from './Values.astro'
import ServicesBand from './ServicesBand.astro'
import Gallery from './Gallery.astro'
import Founder from './Founder.astro'
import Prose from './Prose.astro'
import PullQuote from './PullQuote.astro'
import Cta from './Cta.astro'

const { blocks = [], lang = 'fr' } = Astro.props

const MAP = {
  hero: HeroText,
  values: Values,
  services: ServicesBand,
  gallery: Gallery,
  founder: Founder,
  prose: Prose,
  pullquote: PullQuote,
  cta: Cta,
}
---
{blocks.map((block) => {
  const Comp = MAP[block.blockType]
  return Comp ? <Comp block={block} lang={lang} /> : null
})}
